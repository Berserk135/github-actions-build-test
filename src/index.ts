// Простой код, который успешно компилируется
const greeting: string = "Hello from GitHub Actions!";
console.log(greeting);

// Небольшая функция для демонстрации типизации
function add(a: number, b: number): number {
  return a + b;
}

const result = add(5, 3);
console.log(`5 + 3 = ${result}`);
